using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using HexasphereGrid;

public class InventoryManager : MonoBehaviour
{
  //  public void AddToInventory(KeyValuePair<string, int> resource, Tile tile)
  //  {
  //      if (tile == null)
  //      {
  //          Debug.Log("null tile error");
  //      }
  //      if (tile.inventory.ContainsKey(resource.Key))
  //      {
  //          tile.inventory[resource.Key] += resource.Value;
  //      }
  //      else
  //      {
  //          tile.inventory.Add(resource.Key, resource.Value);
  //      }
  //  }
  //
  //  public static void RemoveFromInventory(KeyValuePair<string, int> resource, Tile tile)
  //  {
  //      if (tile.inventory.ContainsKey(resource.Key) && tile.inventory[resource.Key] >= resource.Value)
  //      {
  //          tile.inventory[resource.Key] -= resource.Value;
  //      }
  //      else
  //      {
  //          Debug.Log("Warning: inventory request error");
  //      }
  //  }
}
